path = "D:\FEI-STU\BAKALARSKA_PRACA\CIRCUITS\";
img = path+"map2.png";
checkpoints = [];
finish = [];
scale = 1;
maxSteps = 1;
map = Map(img, scale, checkpoints, finish, maxSteps);
imshow(map.image)
show(map.map)
car = Car(10, 10, 0, map, 3);
angle = 0;
x = 0;
dt = 0.1;
figure
car.drawCar();
%% 

% xs = zeros(100, 2);
% ys = zeros(100, 2);

while x < 100
    clf
    if dt > 2
        angle = -pi/120;
    else
        angle = pi/120;
    end
    car = car.update(angle, 1, 0);

    car.drawCar();
    pause(0.0001)

    x = x + 1;

    dt = dt + 0.1;
end

plot(xs(:), ys(:), 'rX');
xlim([0, 100])
ylim([0, 100])